# 实验参数与平台表格

## 平台与软件环境

| 类别 | 参数 | 取值 | 来源/说明 |
|---|---|---|---|
| 研究任务 | 课题 | 基于动态功率调节的大语言模型推理能耗控制 | 项目文档与实验总结 |
| 推理框架 | Serving 框架 | vLLM OpenAI 兼容 API | `start_vllm_server.sh` |
| vLLM 版本 | `vllm` | `0.17.0` | 当前 Python 环境与 `requirements.txt` |
| 深度学习框架 | `torch` | 当前环境 `2.10.0+cu128`；依赖文件记录 `2.10.0+cu129` | `python import torch` 与 `requirements.txt` |
| CUDA | CUDA runtime | 当前环境 `12.8` | `torch.version.cuda` |
| Transformers | `transformers` | `4.57.6` | `requirements.txt` |
| 数据处理 | `pandas` / `numpy` | `pandas 3.0.1` / `numpy 2.2.6` | 当前环境与 `requirements.txt` |
| 可视化 | `matplotlib` / `seaborn` | 当前环境 `matplotlib 3.10.8` / `seaborn 0.13.2`；依赖文件记录 `seaborn 0.13.0` | 当前环境与 `requirements.txt` |
| GPU 监测 | NVML 绑定 | `pynvml 11.5.0` | `requirements.txt` |
| GPU 平台 | GPU | 单卡 NVIDIA RTX 4080，16GB 显存，最高功率上限 350W | `idea.md` 实验平台记录；当前会话 `nvidia-smi` 不可用，未做实时复核 |
| GPU 编号 | 主策略实验 | GPU0 | `预填充阶段策略评估.md`、相关 metadata |
| GPU 编号 | Prefill token-power 建模 | GPU1 | `预填充阶段 Token-Power 建模实验总结.md` |
| 功率控制 | 设置方式 | `nvidia-smi -pl`，代码封装于 `power_control.py` | `power_control.py` |
| 功率监测 | 采样方式 | 优先 `pynvml`，失败回退 `nvidia-smi` | `monitor.py` |
| 功率监测 | 默认采样间隔 | `0.1 s` | `PowerMonitor(sample_interval=0.1)` |
| 能耗计算 | 积分方法 | 相邻功率采样点梯形积分，单位 J | `monitor.py` 与各实验脚本 |

## vLLM 服务参数

| 参数 | 取值 |
|---|---|
| 模型路径 | `./Qwen2.5-7B-Instruct-AWQ` |
| Served model name | `Qwen2.5-7B-Instruct-AWQ` |
| Tokenizer | `./Qwen2.5-7B-Instruct-AWQ` |
| 量化方式 | AWQ |
| API 地址 | `http://localhost:8000/v1` |
| Host / Port | `0.0.0.0:8000` |
| GPU memory utilization | `0.85` |
| Chunked prefill | enabled |
| Prefix caching | disabled，`--no-enable-prefix-caching` |
| `max_num_batched_tokens` | `2048` |
| `max_num_seqs` | `64` |
| Scheduler signature | `cp1_mbt2048_mns64`，用于 decode 建模数据标识 |

## 通用数据集与推理参数

| 参数 | 取值 | 说明 |
|---|---|---|
| 数据集 | ShareGPT | 主实验使用真实用户请求 |
| 数据目录 | `./input/ShareGPT` | 多数策略评估实验 metadata 记录 |
| 过滤数据目录 | `./filtered_prompts` | 部分频率扫描实验使用 |
| 解码实验输入 | 1 token 或 ShareGPT 分桶输入 | Decode 策略评估固定 1 token；前馈实验使用 ShareGPT query groups |
| Prefill 隔离设置 | `max_tokens=1` | 用 1 token 输出近似隔离 prefill TTFT |
| Decode 输出设置 | `max_tokens=output_length` | Decode 建模、策略评估、前馈/PID 实验 |
| Temperature | `0.0` | 策略评估脚本统一使用确定性采样 |
| `top_p` | `1.0` | OpenAI API `extra_body` |
| Decode `ignore_eos` | `True` | Decode 建模/策略/前馈实验使用，保证固定生成长度 |
| 常用随机种子 | `20260329`、`20260401`、`20260425`、`20260426` | 不同实验线的 queue/sampling seed |
| 主要指标 | Energy、Avg Power、Peak Power、TTFT、TBT、E2E、Throughput、EDP | 由实验脚本与分析报告输出 |

## 负载分组参数

| 分组用途 | Query count | Target input tokens |
|---|---:|---:|
| Decode power sweep / 纯前馈 / 前馈+PID | 8 | 225 |
| Decode power sweep / 纯前馈 / 前馈+PID | 16 | 504 |
| Decode power sweep / 纯前馈 / 前馈+PID | 32 | 1581 |
| Decode power sweep / 纯前馈 / 前馈+PID | 64 | 2175 |
| Decode power sweep / 纯前馈 / 前馈+PID | 96 | 11106 |
| Decode power sweep / 纯前馈 / 前馈+PID | 128 | 22873 |
| Prefill 策略评估 | 8 | 225 |
| Prefill 策略评估 | 16 | 504 |
| Prefill 策略评估 | 32 | 1581 |
| Prefill 策略评估 | 64 | 2175 |
| Prefill 策略评估 | 103 | 6053 |
| Prefill 策略评估 | 112 | 11106 |
| Prefill 策略评估 | 119 | 20295 |

## 各实验线参数总表

| 实验线 | 数据目录/来源 | 核心变量 | 功率设置 | 采样/重复设置 | 主要用途 |
|---|---|---|---|---|---|
| 静态功率封顶 | `experiment_results/legacy/results0/` | Query count `8,32,64,128`；每请求输出约 `100` tokens | 静态 `150,175,200,250,300,350W` | 91 次有效运行；24 个功率-负载配置；多数重复 4 次，部分 3 次 | 建立固定功率基准，分析能耗-延迟-吞吐折中 |
| Prefill token-power 建模 | `experiment_results/prefill_token_power_modeling/gpu1_350w_block_nocache_fixed_0_20000/` | 输入 token `1-20000`；前段重点 `1-3000`；输出 `1` token | 固定 `350W` | 全范围 45 个 token 长度、174 条有效样本；block mode；prefix cache 关闭 | 建模 prefill 输入长度与功率/TTFT/能耗关系 |
| Prefill 策略评估 | `experiment_results/prefill_concurrent_evaluation/prefill_strategy_gpu0_r50x3/` | Query count `8,16,32,64,103,112,119`；输出 `1` token | `baseline_350w`、`prefill_token_fit`、`prefill_token_fit_plus25w`、`prefill_manual_buckets` | `repeats_per_batch=50`，`full_repeats=3`，`warmup_batches=2`，`monitor_warmup_batches=1` | 选择 prefill 阶段前馈功率桶 |
| Decode 建模 | `experiment_results/decode_modeling/decode_modeling/merged_filtered/` | Batch size `1,2,4,6,8,12,16,24,32,40,48,50,56,60,64`；输出 `10,20,40,50,75,100,150,200,300` | 固定默认高功率，建模阶段不比较功率策略 | 135 个聚合配置；762 条 repeat 记录；22495 个聚合请求 | 建模 decode power、TBT 与 batch/output/KV pressure 的关系 |
| Decode power cap sweep | `experiment_results/decode_power_cap_batch_sweep/multi_load_tbt5_out100/` | Query count `8,16,32,64,96,128`；输出 `100` | Decode power cap `150,160,170,180,190,210,230,250,350W` | `repeats_per_cap=5`，`warmup_batches=1`，`monitor_warmup_batches=1`，`inter_batch_sec=0.3` | 为 out100 选择满足 TBT 约束的 decode 功率桶 |
| Decode power cap sweep | `experiment_results/decode_power_cap_batch_sweep/multi_load_tbt5_out200/` | Query count `8,16,32,64,96,128`；输出 `200` | Decode power cap `150,160,170,180,190,210,230,250,350W` | `repeats_per_cap=5`，`warmup_batches=1`，`monitor_warmup_batches=1`，`inter_batch_sec=0.3` | 为 out200 选择满足 TBT 约束的 decode 功率桶 |
| Decode 策略评估 | `experiment_results/decode_strategy/strategy_evaluation_policy_retry_q8/` 与 `..._q16/` | 输入长度固定 `1` token；concurrency `8,16`；输出 `8,16,32,64,96,128` | `scheme1_fit_curve`、`scheme2_fit_plus`、`scheme3_kv_guided`、`baseline_350w` | `repeats_per_batch=50`，`full_repeats=3`，`warmup_batches=3`，`monitor_warmup_batches=1`；合计 7200 行 batch 原始数据 | 比较 decode 阶段候选功率映射 |
| 纯前馈 out100 | `experiment_results/feedforward/final_guarded_out100_r50x3/` | Query count `8,16,32,64,96,128`；输出 `100` | Prefill `350W`；decode `190/205/210W` guarded 桶；baseline `350W` | `repeats_per_batch=50`，`full_repeats=3`，`warmup_batches=2`，`monitor_warmup_batches=1` | 验证纯前馈控制在 out100 下的节能与延迟代价 |
| 纯前馈 out200 | `experiment_results/feedforward/final_guarded_out200_r50x3_retry/` | Query count `8,16,32,64,96,128`；输出 `200` | Prefill `350W`；decode `190/205/210W` guarded 桶；baseline `350W` | `repeats_per_batch=50`，`full_repeats=3`，`warmup_batches=2`，`monitor_warmup_batches=1` | 验证纯前馈控制在 out200 下的节能与延迟代价 |
| 前馈+PID out100 | `experiment_results/feedforward/pid_guard_energy_first_out100_r50x3/` | Query count `8,16,32,64,96,128`；输出 `100` | Prefill/decode 前馈基线叠加 PID guard；baseline `350W` | `repeats_per_batch=50`，`full_repeats=3`，`warmup_batches=2`，`monitor_warmup_batches=1` | 在纯前馈基础上降低 TBT/E2E 性能代价 |
| 前馈+PID out200 | `experiment_results/feedforward/pid_guard_energy_first_out200_r50x3/` | Query count `8,16,32,64,96,128`；输出 `200` | Prefill/decode 前馈基线叠加 PID guard；baseline `350W` | `repeats_per_batch=50`，`full_repeats=3`，`warmup_batches=2`，`monitor_warmup_batches=1` | 在纯前馈基础上降低 TBT/E2E 性能代价 |

## 最终策略参数表

| 阶段 | 策略 | 负载条件 | 功率上限 |
|---|---|---|---:|
| Baseline | `baseline_350w` | 所有阶段 | 350W |
| Prefill | 简化分段桶 | `q <= 16` | 200W |
| Prefill | 简化分段桶 | `32 <= q <= 64` | 220W |
| Prefill | 简化分段桶 | `q >= 103` | 260W |
| Decode | `scheme3_kv_guided` / guarded decode | 小负载或短输出桶 | 190W |
| Decode | `scheme3_kv_guided` / guarded decode | 中等输出/负载桶 | 205W |
| Decode | `scheme3_kv_guided` / guarded decode | 长输出或高负载桶 | 210W |
| 纯前馈 out100/out200 | `ff_decode_tbt_guarded` | `q=8,16,32` | 190W |
| 纯前馈 out100/out200 | `ff_decode_tbt_guarded` | `q=64,96` | 205W |
| 纯前馈 out100/out200 | `ff_decode_tbt_guarded` | `q=128` | 210W |

## PID Guard 参数表

| 参数 | 取值/说明 |
|---|---|
| 策略名 | `ff_decode_tbt_guarded_pid` |
| 目标 | 在前馈功率桶基础上，利用反馈降低 TBT/E2E 代价 |
| 对比 baseline | `baseline_350w` |
| 负载 | Query count `8,16,32,64,96,128` |
| 输出长度 | `100` 与 `200` |
| 样本设置 | 每输出长度每策略 900 个样本 |
| 控制输入 | TBT/TTFT 目标与运行时反馈 |
| 论文展示口径 | out100 与 out200 分别报告 GEOMEAN energy saving、TBT increase、TTFT increase、E2E increase |

## 图表与分析输出参数

| 项目 | 取值 |
|---|---|
| 主要图像格式 | PNG + PDF |
| 论文图 DPI | `600` |
| 主要分析脚本 | `analyze_results.py`、`analyze_prefill_modeling.py`、`analyze_prefill_concurrent_evaluation.py`、`analyze_decode_strategy_evaluation.py`、`analyze_feedforward_evaluation.py` |
| 主要输出目录 | `experiment_results/**/images/`、`results*/` |
| 主要报告文件 | `静态功率封顶实验.md`、`预填充阶段 Token-Power 建模实验总结.md`、`预填充阶段策略评估.md`、`解码阶段建模.md`、`解码阶段策略评估.md`、`纯前馈控制器.md`、`前馈+pid策略评估.md` |

## 备注

| 项目 | 说明 |
|---|---|
| 平台实时复核 | 当前会话执行 `nvidia-smi --query-gpu=...` 失败，错误为无法与 NVIDIA driver 通信。因此本表中的 GPU 型号与功率上限来自实验记录，不来自当前实时查询。 |
| 统计口径 | 策略评估中的相对指标均以同一实验目录内的 `baseline_350w` 为基准，不跨目录混用 baseline。 |
| 能耗口径 | 能耗由 GPU 功率采样积分得到，平均功率、显存、温度、SM clock、memory clock 均来自监测采样。 |
