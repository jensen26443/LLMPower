# LLM功率控制实验系统 - 依赖包
# 从本地环境导出 (2026-03-10)

# 核心依赖
vllm==0.17.0
torch==2.10.0+cu129
transformers==4.57.6

# 数据处理
pandas==3.0.1
numpy==2.2.6

# 可视化
matplotlib==3.10.8
seaborn==0.13.0

# 工具
tqdm==4.66.0
pynvml==11.5.0

# OpenAI 兼容客户端
openai&gt;=1.0.0

# 注意：
# - torch版本可能需要根据服务器CUDA版本调整
# - 如果服务器CUDA版本不同，可以删除torch==2.10.0+cu129行，
#   让vllm自动安装兼容的torch版本
